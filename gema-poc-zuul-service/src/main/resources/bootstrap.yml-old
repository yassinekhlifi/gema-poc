spring:
  application:
    name: gateway-service
  jmx:
    default-domain: gateway-service
#  profiles:
# add -Dspring.profiles.active=prod in VM arguments to force profile
#    active: dev
cloud:
   config:
      uri: http://localhost:8888

management:
  security:
    enabled: false
