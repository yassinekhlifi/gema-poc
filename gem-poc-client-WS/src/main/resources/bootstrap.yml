server:
  port: 8669
spring:
  application:
    name: images-service-client
  cloud:
    config:
      uri: http://localhost:8888
  sleuth:
    sampler:
      probability: 1
  zipkin:
    base-url: ${zipkin.baseUrl}
